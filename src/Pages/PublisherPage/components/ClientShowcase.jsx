import ClientShowcase from '../../../components/ClientShowcase';

const PublisherClientShowcase = () => {
    return (
        <ClientShowcase
            title="A new interaction signal layer for streaming"
            subtitle="Adopted across the streaming ecosystem"
        />
    );
};

export default PublisherClientShowcase;
